package com.skd.betterconnections;

import net.neoforged.api.distmarker.Dist;
import net.neoforged.fml.common.Mod;

@Mod(value = BetterConnections.MODID, dist = Dist.CLIENT)
public class BetterConnectionsClient {
    public BetterConnectionsClient() {
    }
}
